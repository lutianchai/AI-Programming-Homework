# 课程作业 03：人脸识别系统

本项目是《人工智能通识》课程的第三次作业。利用 `face_recognition` 库实现人脸特征提取，并结合 `Streamlit` 搭建交互式 Web 界面。

## 🛠️ 环境要求
- Python 3.13
- CMake (用于编译 dlib)
- 依赖库：`face_recognition`, `streamlit`, `opencv-python`, `numpy`

## 🚀 如何运行
1. 安装依赖：
   ```bash
   pip install -r requirements.txt
## 🧑‍💻 在macOS下运行良好
